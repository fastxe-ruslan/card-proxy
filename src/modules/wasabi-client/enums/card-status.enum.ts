export enum CardStatus {
  Active = 'active',
  Pending = 'pending',
  Frozen = 'frozen',
  Reject = 'reject',
  Cancelled = 'cancelled',
}

export enum CardType {
  Virtual = 'virtual',
  Physical = 'physical',
}

export enum IdType {
  Passport = 'PASSPORT',
  NationalId = 'NATIONAL_ID',
  DriverLicense = 'DRIVER_LICENSE',
}
